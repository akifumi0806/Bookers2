<div class="container">
	<div class="row">

<%= render 'users/userinfo', user:@user %>

<%= render 'users/newbook', book:@book %>

    	<div class="col-md-9">
    		<h2>Book detail</h2>
    			<table class = "table table-hover table-inverse">
  			
  				<tbody>
              		<% @books.each do |book| %>
      					<tr>
                    		<td><%= attachment_image_tag @user, :image,　format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %></td>
        					<td><%= book.title %></td>
              				<td><%= book.body %></td>
              				<td><%= link_to 'Edit', edit_book_path(book.id), class: "btn-sm btn-success" %></td>
              				<td><%= link_to 'Destroy', book_path(book.id),  class: "btn-sm btn-danger" %></td>
            			</tr>
              		<% end %>
 				</tbody>
				</table>
    	</div>