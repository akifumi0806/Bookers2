<div class="container">
	<div class="row">

<%= render 'users/userinfo', user:@user %>

<%= render 'users/newbook', book:Book.new %>

    	<div class="col-md-9">
    		<h2>Book detail</h2>
    			<table class = "table table-hover table-inverse">
  			
        
  				<tbody>
      					<tr>
                    	<td><%= attachment_image_tag @user, :image,　format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %><br>
                        <%=link_to users_path do %><%= @user.name %><% end %></td>
        					    <td><%= @book.title %></td>
              				<td><%= @book.body %></td>
              				<td><%= link_to 'Edit', edit_book_path(@book.id), class: "btn-sm btn-success" %></td>
              				<td><%= link_to 'Destroy', destroy_book_path(@book.id), method: :delete,  class: "btn-sm btn-danger" %></td>
            			</tr>
 				</tbody>
				</table>
    	</div>