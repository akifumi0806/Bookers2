<h2>User info</h2>
			<%= form_for @mypage do |f| %>

			<h4>Name</h4>
    		<%= f.string_field :name %>

    		<h4>Introduction</h4>
    		<%= f.string_area :introduction %>

    		<%= f.submit 'マイページ編集', class: "btn btn-primary btn-lg btn-block" %>
    		<% end %>